import { ExpansionDirective } from '@core/derectives/expansion/drop-list.directive';

describe('DropListDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
